---
plan: 202607/project_display_names.md
---
 The `gh_sase-org__sase` directory basename is being used instead of the real project name, which is `sase`, again (see #sshot). Can you help me fix this and make sure there are no other similar bugs throughout this codebase? Think this through thoroughly and create a plan using your `/sase_plan` skill. Choose and author the appropriate
tier, validate and revalidate until it passes, then submit it with `sase plan propose` (as the skill instructs)
before making any file changes.
