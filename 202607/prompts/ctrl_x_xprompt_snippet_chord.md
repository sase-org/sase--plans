---
plan: 202607/ctrl_x_xprompt_snippet_chord.md
---
 Can you help me change the `<ctrl+t>` (snippet) keymap trigger in the "Save draft as xprompt" panel (triggered via the `<ctrl+g>x` keymap in the prompt input widget) to `<ctrl+x>` and also allow `<ctrl+g><ctrl+x>` to be used as an alias for `<ctrl+g>x` so the user can just press `<ctrl+g><ctrl+x><ctrl+x>` to trigger this functionality? Think this through thoroughly and create a plan using your `/sase_plan` skill. Choose and author the appropriate
tier, validate and revalidate until it passes, then submit it with `sase plan propose` (as the skill instructs)
before making any file changes.
