- **PLAN:** [../202607/restart_individual_family_members.md](../restart_individual_family_members.md)

 Can you help me add support for killing and restarting individual members of agent families from the agents tab using the `,x` keymap? For example, in #sshot, pressing `,x` should dismiss the `sase-8u.4.2--code` agent and load the proper prompt (using `!` to overwrite the agent name) in the prompt input widget. Think this through thoroughly and create a plan using your `/sase_plan` skill. Choose and author the appropriate
tier, validate and revalidate until it passes, then submit it with `sase plan propose` (as the skill instructs)
before making any file changes.
