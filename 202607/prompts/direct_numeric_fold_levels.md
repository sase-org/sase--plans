---
plan: 202607/direct_numeric_fold_levels.md
---
 Can you help me add new `z<N>` keymaps to the agents tab (and anywhere else that we support folding with `z` keymaps), where `<N>` is some valid fold level given the context? This keymap should then switch to that level of folding. For example, on the agents tab, when an agent tribe panel is selected, the `z1`, `z2`, `z3`, and `z4` keymaps should be defined. Think this through thoroughly and create a plan using your `/sase_plan` skill. Choose and author the appropriate
tier, validate and revalidate until it passes, then submit it with `sase plan propose` (as the skill instructs)
before making any file changes.
