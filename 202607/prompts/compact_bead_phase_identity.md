---
plan: 202607/compact_bead_phase_identity.md
---
 Can you help me get rid of the `ID:` field shown in the `BEAD` lane (see #sshot) and just put the bead ID next to the word `phase` (which is to the right of `BEAD`)? Think this through thoroughly and create a plan using your `/sase_plan` skill. Choose and author the appropriate
tier, validate and revalidate until it passes, then submit it with `sase plan propose` (as the skill instructs)
before making any file changes.
 