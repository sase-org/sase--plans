- **PLAN:** [../202607/common_placeholder_tags.md](../common_placeholder_tags.md)

 Can you help me start saving all `<foobar>` tags that I write (up to the common words limit, configured via a sase config field) in prompts and showing them when `<foobar>` completion is triggered? Make sure these common tags are shown beneath the tags that are in the current prompt and they are visually distinct somehow so the user can easily tell which tags in the completion menu are from the current prompt and which are from the saved list of common tags which at a glance.

I want you to lead the design on this one. Make sure you design this feature so it is intuitive, reliable, and (last but not least) beautiful! Think this through thoroughly and create a plan using your `/sase_plan` skill. Choose and author the appropriate
tier, validate and revalidate until it passes, then submit it with `sase plan propose` (as the skill instructs)
before making any file changes.
