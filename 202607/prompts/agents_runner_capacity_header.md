---
plan: 202607/agents_runner_capacity_header.md
---
 Can you help me start showing the currently configured max runner limit
on the "Agents" tab of the `sase ace` TUI?

- Let's put this next to the total agent count that is shown at the top.
- Also, let's start showing how many agents are queued (i.e. waiting for some
  agents to finish so the max agent runners limit won't be violated if they
  launch) here too.
- I want you to lead the design on this one. Make sure you design this feature so it is intuitive, reliable, and (last but not least) beautiful!

Think this through thoroughly and create a plan using your `/sase_plan` skill. Choose and author the appropriate
tier, validate and revalidate until it passes, then submit it with `sase plan propose` (as the skill instructs)
before making any file changes.
