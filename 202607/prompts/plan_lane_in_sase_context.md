---
plan: 202607/plan_lane_in_sase_context.md
---
 Can you help me merge the `SASE PLAN` section into the `SASE CONTEXT` section (see #sshot for what these look like now) by adding a new `PLAN` sub-section to the `SASE CONTEXT` section? I want you to lead the design on this one. Make sure you design this feature so it is intuitive, reliable, and (last but not least) beautiful! Think this through thoroughly and create a plan using your `/sase_plan` skill. Choose and author the appropriate
tier, validate and revalidate until it passes, then submit it with `sase plan propose` (as the skill instructs)
before making any file changes.
  