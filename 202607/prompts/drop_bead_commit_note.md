- **PLAN:** [../202607/drop_bead_commit_note.md](../drop_bead_commit_note.md)

 Can you help me stop adding the `COMMIT: <sha>` note to beads? This never seems to point to the right commit and (more importantly) overwrites any notes that were left previously (which sase agents sometimes do while working beads). Think this through thoroughly and create a plan using your `/sase_plan` skill. Choose and author the appropriate
tier, validate and revalidate until it passes, then submit it with `sase plan propose` (as the skill instructs)
before making any file changes.
