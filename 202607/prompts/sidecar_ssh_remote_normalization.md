---
plan: 202607/sidecar_ssh_remote_normalization.md
---
 It looks like sidecar repos are being initialized (or were at some point) using HTTPS instead of SSH. Can you help me confirm/deny this suspision and fix the issue? Think this through thoroughly and create a plan using your `/sase_plan` skill. Choose and author the appropriate
tier, validate and revalidate until it passes, then submit it with `sase plan propose` (as the skill instructs)
before making any file changes.
