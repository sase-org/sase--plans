- **PLAN:** [../202607/beads_sidecar_repo.md](../beads_sidecar_repo.md)

 Can you help me move the beads/ directory out to its own `beads`
sidecar repo (this should be done for all future projects, but you should also
make sure to migrate all of my currently enabled sase projects)?

- See the `mg.cld` sase agent's chat for some context and motivation behind this
  change.
- The beads/ directory's current contents should be the contents of the new
  `beads` sidecar repos.
- This repo should be auto-cloned, like the `plans` sidecar repo currently is.
- Make sure this new repo is initialized by the `sase repo init` command (and
  the `sase init` command, by extension) with an excellent README.md file
  containing an excellent infographic (generated by GPT image).
- Make sure that `sase bead` commands still work when the sase/repos/beads/
  directory doesn't exist by cloning it (see how the `sase repo open` command
  does this) to the sase/repos/beads/ directory.
- I want you to lead the design on this one. Make sure you design this feature so it is intuitive, reliable, and (last but not least) beautiful!

Think this through thoroughly and create a plan using your `/sase_plan` skill. Choose and author the appropriate
tier, validate and revalidate until it passes, then submit it with `sase plan propose` (as the skill instructs)
before making any file changes.
 