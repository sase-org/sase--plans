- **PLAN:** [../202607/visual_surround_keymap.md](../visual_surround_keymap.md)

 Can you help me add support for the `S` keymap visual-model keymap in the prompt input widget, inspired by vim-surround, to complement the `ys` normal-mode keymap that is already implemented? Think this through thoroughly and create a plan using your `/sase_plan` skill. Choose and author the appropriate
tier, validate and revalidate until it passes, then submit it with `sase plan propose` (as the skill instructs)
before making any file changes.
