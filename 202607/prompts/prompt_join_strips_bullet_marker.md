- **PLAN:** [../202607/prompt_join_strips_bullet_marker.md](../prompt_join_strips_bullet_marker.md)

 Can you help me have the `J` keymap, which joins the next line with the current one in the prompt input widget, start auto-removing the `- ` prefix from the next line, assuming the next line starts with some optional whitespace followed by `- `? Think this through thoroughly and create a plan using your `/sase_plan` skill. Choose and author the appropriate
tier, validate and revalidate until it passes, then submit it with `sase plan propose` (as the skill instructs)
before making any file changes.
 