- **PLAN:** [../202607/capitalized_snippet_aliases.md](../capitalized_snippet_aliases.md)

 I often want to use a sase snippet exactly as it is defined, but with the first letter of its contents capitalized. Can you help me add support for this by automatically defining a sase snippet of the same name but with the first letter of the name capitalized for all sase snippets? For example, a sase snippet named `foo` that expands to `foo bar baz` should cause a sase snippet `Foo` to be defined as well, which should expand to `Foo bar baz`. Think this through thoroughly and create a plan using your `/sase_plan` skill. Choose and author the appropriate
tier, validate and revalidate until it passes, then submit it with `sase plan propose` (as the skill instructs)
before making any file changes.
