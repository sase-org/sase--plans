---
plan: 202607/restore_agents_status_priority.md
---
 Why are `Stopped` and `Waiting` agents shown at the bottom instead of at the top and below the `Running` group, respectively, when grouping the agents tab `by status` (see #sshot)? I'm guessing that we broke something when we changed how we sort agents within an agent clan recently. Can you help me diagnose the root cause of this issue and fix it? Think this through thoroughly and create a plan using your `/sase_plan` skill. Choose and author the appropriate
tier, validate and revalidate until it passes, then submit it with `sase plan propose` (as the skill instructs)
before making any file changes.
