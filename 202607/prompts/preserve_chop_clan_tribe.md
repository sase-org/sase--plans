---
plan: 202607/preserve_chop_clan_tribe.md
---
 The toobig chop's agents are supposed to run in the `@chop` tribe. Can you help me fix this by making the `toobig-@` clan join the `@chop` agent tribe? Think this through thoroughly and create a plan using your `/sase_plan` skill. Choose and author the appropriate
tier, validate and revalidate until it passes, then submit it with `sase plan propose` (as the skill instructs)
before making any file changes.
