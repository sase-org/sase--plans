---
plan: 202607/tail_preserving_tool_call_output.md
---
 We do not currently truncate the tool output shown in tool call files (ex: /home/bryan/.sase/tool_call_reports/bash-172749-10030f8d.md) that are created when the user uses the `v` (view) keymap on the agents tab to inspect a slow tool call. Can you help me start truncating the beginning of this output instead of the end and always show at least the last 50 lines of the command's output? Think this through thoroughly and create a plan using your `/sase_plan` skill. Choose and author the appropriate
tier, validate and revalidate until it passes, then submit it with `sase plan propose` (as the skill instructs)
before making any file changes.
