- **PLAN:** [../202607/prompt_inputs_show_full_value.md](../prompt_inputs_show_full_value.md)

 Can you help me fix the panel that pops up when placeholders are found in a prompt submitted from the prompt input widget? Namely, we need to make it so the full, potentially multiline, input for each placeholder is shown at all times (see #sshot to get an idea of the problem--I have `Make PreviewPanelModal a real reader — M, highest daily value per line changed` typed in for that placeholder). Think this through thoroughly and create a plan using your `/sase_plan` skill. Choose and author the appropriate
tier, validate and revalidate until it passes, then submit it with `sase plan propose` (as the skill instructs)
before making any file changes.
 