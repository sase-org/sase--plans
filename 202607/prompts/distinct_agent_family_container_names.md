- **PLAN:** [../202607/distinct_agent_family_container_names.md](../distinct_agent_family_container_names.md)

 This doens't look right (see #sshot). An agent family and an agent should never be allowed to share the same name. The agent family should be named `hc.f0.f0`, not `hc.f0.f0--0`. Can you help me fix this? Think this through thoroughly and create a plan using your `/sase_plan` skill. Choose and author the appropriate
tier, validate and revalidate until it passes, then submit it with `sase plan propose` (as the skill instructs)
before making any file changes.
