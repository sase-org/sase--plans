- **PLAN:** [../202607/todo_running_gold.md](../todo_running_gold.md)

 Can you help me start using the same yellow color for the todo syntax highlighting background color (instead of the pinkish orange that we use today) that we do for the foreground of the `RUNNING` agent status on the agents tab (both of these are shown in #sshot)? Make sure the text remains readable (preferably make it more readable because it's not very readable right now). Think this through thoroughly and create a plan using your `/sase_plan` skill. Choose and author the appropriate
tier, validate and revalidate until it passes, then submit it with `sase plan propose` (as the skill instructs)
before making any file changes.
