---
plan: 202607/collapsed_agent_panels_last.md
---
 Can you help me always sort collapsed agent panels on the agents tab at the very bottom of the agent panel list? For example, in #sshot, the `#chop` agent panel/group should be at the bottom, instead of one from the top. Think this through thoroughly and create a plan using your `/sase_plan` skill. Choose and author the appropriate
tier, validate and revalidate until it passes, then submit it with `sase plan propose` (as the skill instructs)
before making any file changes.
 