---
plan: 202607/tribe_agent_count_projection.md
---
 The `TRIBE MEMBERS` section is still showing invalid agent status counts and invalid agent counts (ex: there are 10 agents in the agent tribe selected in #sshot, not 12--i.e. 2 top-level agents and 4 agent families each containing 2 agents == 10 total agents, not 12 total agents). Can you help me fix these issues and any others that you see in the screenshot file I provided? Think this through thoroughly and create a plan using your `/sase_plan` skill. Choose and author the appropriate
tier, validate and revalidate until it passes, then submit it with `sase plan propose` (as the skill instructs)
before making any file changes.
 