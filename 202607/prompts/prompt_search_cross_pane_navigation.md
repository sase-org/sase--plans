---
plan: 202607/prompt_search_cross_pane_navigation.md
---
 We recently (a day or so ago) added support to the prompt input widget's `n` / `N` keymaps for matching the last search query across multiple different prompt input widgets; however, when these keymaps are used and the next match we should jump to lives in the next/prev prompt input widget, we are supposed to jump to that prompt input widget and then jump to that match. We don't currently do this. Can you help me fix this? Think this through thoroughly and create a plan using your `/sase_plan` skill. Choose and author the appropriate
tier, validate and revalidate until it passes, then submit it with `sase plan propose` (as the skill instructs)
before making any file changes.
