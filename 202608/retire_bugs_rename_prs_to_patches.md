---
tier: tale
title: Retire Bugs, rename PRs to Patches, reorder the Artifacts sub-tabs
goal:
  The ACE Artifacts tab presents exactly four sub-tabs — Stitches, Patches, Beads, Files
  — with the Bugs pane and its actions, keymaps, copy targets, and goldens deleted, the
  `prs` identifier renamed to `patches` end to end, deprecated aliases keeping saved
  state and existing configs loading, and every affected text and PNG golden
  regenerated.
size: medium
proposed_by: bbugyi200.athena.sase-jd.8
bead: sase-jd.8
create_time: 2026-08-11 08:28:59
status: wip
---

- **PARENT:**
  [202608/external_artifact_ingestion.md](https://github.com/sase-org/sase--plans/blob/main/202608/external_artifact_ingestion.md)
- **BEAD:**
  [sase-jd.8](https://github.com/sase-org/sase--beads/blob/main/pages/sase-jd/sase-jd.8.md)

# Retire Bugs, rename PRs to Patches, reorder the Artifacts sub-tabs

Phase `tabs` of epic `sase-jd` (bead `sase-jd.8`). The epic plan is
`@plan:202608/external_artifact_ingestion.md`; read its `Phase tabs` section and its
`Ground rules for every phase` before starting.

The Artifacts sub-tab strip becomes exactly four panes:

```text
  STITCHES     PATCHES     BEADS     FILES
   #FFD700     #00D7AF   #D787FF   #FFAF5F
```

Three things happen together, in one landed change, because the sub-tab set, the number
keys, and the goldens cannot be split without leaving the TUI in a wrong intermediate
state:

1. The Bugs sub-tab and its pane, actions, keymaps, copy targets, and goldens are
   deleted.
2. The `prs` sub-tab identifier is renamed to `patches` end to end.
3. `ARTIFACTS_SUBTAB_ORDER` becomes `("stitches", "patches", "beads", "files")`.

## Hard preconditions — already verified, re-confirm cheaply before editing

The epic plan forbids starting this phase until both hold. Both were checked while this
plan was written; re-confirm each with the one-line check below rather than repeating
the full audit.

1. **`sase-j8` has landed.** `sase bead show sase-j8` reports `[CLOSED]`, and
   `_ARTIFACT_LABELS` in `src/sase/ace/tui/widgets/artifacts/view.py` already reads
   `"stitches": "Stitches"`. If that label still says `Commits`, stop and report — the
   Commits→Stitches rename belongs to `sase-j8.4`, never to this phase.

2. **`issue_mirror` has run clean against real projects and the bead list is a superset
   of the tracker issue list.** Verified: every file in
   `~/.sase/external_mirror/issues/` (`gh_sase-org__sase`, `gh_bbugyi200__actstat`,
   `gh_bobs-org__bob-cli`) has `"backfill_complete": true` and `"failures": 0`;
   `gh issue list --state all` returns exactly one issue across all three enabled
   projects (`sase-org/sase#241`); and bead `sase-jp` mirrors it, carrying both
   `external_ref: bug:gh_sase-org__sase#241` and `refs: ["bug:sase#241"]`. Re-confirm
   with:

   ```bash
   cat ~/.sase/external_mirror/issues/*.json | grep -E 'backfill_complete|failures'
   sase bead list -s all -f json -n 0 | python -c "import json,sys; print([(i['id'], i['external_ref']) for i in json.load(sys.stdin)['results'] if i.get('external_ref')])"
   ```

   If a project shows `backfill_complete: false`, non-zero `failures`, or a tracker
   issue with no covering bead, stop and report instead of deleting the Bugs pane.

Note: this phase deletes the last UI that lists remote-only issues. That is intentional
and safe only because of precondition 2 — the Beads pane's external-issue chip
(`sase-jd.6`) plus the mirror (`sase-jd.4`) already cover every issue.

## Ground rules

- **Do not edit SASE memory files** (`sase/memory/*.md`, `AGENTS.md`, `CLAUDE.md`,
  `GEMINI.md`, `OPENCODE.md`, `QWEN.md`). This plan is not user permission.
- **Read `sase/memory/symvision.md` with `/sase_memory_read` before running the lint
  gate.** Dismantling a pane orphans public symbols and symvision will fail otherwise.
- **Run `just install` first** in the ephemeral workspace, then `just check`, and
  `just check-full` before landing.
- No new CLI subcommands or options are added, so `sase/memory/cli_rules.md` is not
  needed. No new glyphs are introduced, so the audited glyph set is untouched.
- `CHANGELOG.md` is generated by release-please — describe the change in the commit
  message, never by editing that file.

## Step 1 — Retire the Bugs pane

### Delete outright

- `src/sase/ace/tui/widgets/artifacts/bugs.py` (`ArtifactsBugsPane`, `BugIssueList`,
  `BugLinkList`)
- `src/sase/ace/tui/widgets/artifacts/bugs_rendering.py` (`BUG_ACCENT`, `issue_row`,
  `issue_meta`) — its only importers are `bugs.py` and the deleted visual test
- `src/sase/ace/tui/actions/artifact_bugs.py` (`ArtifactBugsMixin`,
  `BUG_ARTIFACT_ACTIONS`, `bug_agent_prompt`, `resolved_bug_url`)
- `tests/ace/tui/test_artifacts_bugs.py`,
  `tests/ace/tui/test_artifacts_bugs_backend.py`,
  `tests/ace/tui/visual/test_ace_png_snapshots_artifacts_bugs.py`
- `tests/ace/tui/visual/snapshots/png/artifacts_bugs_populated_120x40.png`,
  `artifacts_bugs_unsupported_120x40.png`, `artifacts_bugs_empty_120x40.png`

### Do NOT delete `src/sase/ace/tui/artifacts_bugs.py` — rename it

The epic plan lists `ace/tui/artifacts_bugs.py` for deletion, subject to its own caveat:
"anything `bead_bug_ui` still needs must have been _moved_, not left behind — check
before deleting." The check says keep it. `sase-jd.6` did not move this module; it
imported it. It is the surviving external-issue provider backend, consumed by four live
callers outside the Bugs pane:

- `src/sase/ace/tui/widgets/artifacts/beads_data.py` — `issue_tracker_capabilities`,
  `list_project_issues`, `resolve_issue_tracker_scope`
- `src/sase/ace/tui/actions/_artifacts_beads_work.py` — `create_project_issue`,
  `update_project_issue`, `issue_url_for_number`
- `src/sase/artifact_cli/open.py` and `src/sase/artifact_ref_prompt_rendering.py` —
  `issue_url_for_number`

Because the name would otherwise refer to a sub-tab that no longer exists, `git mv` it
to `src/sase/ace/tui/external_issues.py`, update its module docstring to describe the
external-issue provider backend, and repoint the four importers plus the monkeypatch
target in `tests/main/test_artifact_cli_open.py`. All six references are static imports
— verify with `rg -n 'artifacts_bugs' src/ tests/` returning nothing afterwards.

Delete from that module only the symbols that die with the pane: `BugSnapshot` and
`collect_bug_snapshot` are imported solely by `widgets/artifacts/bugs.py`; drop them and
any private helper (`_resolve_bug_scope`, `_links_for_issue`, `_merge_bug_links`,
`_load_project_beads`, …) that no other surviving symbol calls. Re-check each candidate
independently rather than deleting a block — `_mutation_scope` / `_url_scope` /
`_provider_bool` still serve the beads callers.

### Edit — sub-tab wiring

- `src/sase/ace/tui/artifact_tabs.py`: drop `"bugs"` from `ArtifactsSubTab`,
  `ArtifactsPaneKey`, `ARTIFACTS_SUBTAB_ORDER`, `ARTIFACTS_PANE_IDS`, and
  `ARTIFACTS_ACCENTS`. `EXTERNAL_ACCENT = "#FF5F5F"` already exists as a named constant
  (landed by `sase-jd.6`) and stays exported — only the `"bugs": EXTERNAL_ACCENT`
  accent-map entry is removed, so the accent survives with a precise meaning instead of
  a dead sub-tab key.
- `src/sase/ace/tui/widgets/artifacts/view.py`: drop the `ArtifactsBugsPane` import, its
  `compose()` yield, the `_ARTIFACT_LABELS["bugs"]` and `_DETAIL_SCROLL_IDS["bugs"]`
  entries, and the two `query_one(ArtifactsBugsPane)` calls in `set_keymap_registry` and
  `set_project_scope`.
- `src/sase/ace/tui/widgets/artifacts/panes.py`: drop `_PLACEHOLDER_COPY["bugs"]`.
- `src/sase/ace/tui/styles.tcss`: drop the `#artifacts-bugs-pane` selectors (the shared
  pane rule near line 80 and the placeholder-card rule near line 359).
- `src/sase/ace/tui/actions/_state_init.py`: drop `"bugs": set()` from
  `_artifacts_marked_targets`.
- `src/sase/ace/tui/widgets/artifacts/__init__.py` / `.pyi`,
  `src/sase/ace/tui/widgets/__init__.py` / `.pyi`: drop the `ArtifactsBugsPane` lazy-map
  entries, `__all__` entries, and re-exports.

### Edit — actions, availability, keymaps, commands

- `src/sase/ace/tui/actions/artifacts_navigation.py`: drop `"bugs"` from the pane-key
  tuple at `:174`. **Keep `action_show_artifacts_bugs` as a deprecated alias** that
  calls `self._switch_artifacts_subtab("beads")` — saved UI state, command-palette
  entries, and muscle memory must not hard-fail. Document the alias in its docstring.
- `src/sase/ace/tui/actions/artifacts.py`: drop the `artifact_bugs` import and
  `BUG_ARTIFACT_ACTIONS` from `NON_PRS_ARTIFACT_ACTIONS`. The set comprehension over
  `ARTIFACTS_SUBTAB_ORDER` no longer yields `show_artifacts_bugs` or
  `show_artifacts_prs`, so add both explicitly so the alias actions stay reachable from
  a non-Patches pane.
- `src/sase/ace/tui/actions/__init__.py` / `.pyi` and `src/sase/ace/tui/app.py`: drop
  `ArtifactBugsMixin` from the lazy map, the stub, and the `AceApp` bases.
- `src/sase/ace/tui/_app_action_availability.py`: drop the `BUG_ARTIFACT_ACTIONS` import
  and its branch, `"bugs"` from the `edit_query` guard and from the pane-refresh set,
  and keep `show_artifacts_bugs` in the `show_artifacts_*` set so the alias remains
  available on the Artifacts tab.
- `src/sase/ace/tui/keymaps/app_keymaps.py`: delete the twelve Bugs sub-tab fields
  (`next_bug`, `prev_bug`, `cycle_bug_filter`, `create_bug`, `edit_bug`,
  `toggle_bug_state`, `open_bug`, `copy_bug`, `start_agent_from_bug`, `focus_bug_links`,
  `activate_bug_link`, `refresh_bugs`). Keep `beads_open_bug`, `beads_copy_bug`, and
  `start_bead_issue_mode` — those are the Beads pane's, landed by `sase-jd.6`.
- `src/sase/ace/tui/keymaps/registry.py`: add those twelve ids to `_RETIRED_APP_KEYS` so
  an existing user override is dropped quietly instead of logging "Unknown keymap
  action". This is the established retirement mechanism (`plans_open_bug` and friends
  are already there).
- `src/sase/default_config.yml`: delete the `# Bugs sub-tab` keymap block and the
  `artifacts_bugs` copy-mode group.
- `src/sase/ace/tui/keymaps/metadata.py` and `src/sase/ace/tui/bindings.py`: delete the
  matching binding metadata and `Binding(...)` entries.
- `src/sase/ace/tui/commands/types.py`: drop `"bugs"` from the `artifacts_subtab`
  Literal. Keep the `"Bugs"` command _category_ — `start_bead_issue_mode` still uses it
  for the Beads pane's external-issue actions.
- `src/sase/ace/tui/commands/availability.py`: delete the twelve `app.<bug action>` ids
  and the `ctx.artifacts_subtab == "bugs"` branch; keep `app.beads_open_bug` /
  `app.beads_copy_bug`.
- `src/sase/ace/tui/commands/_app_metadata.py`: delete the `# Bugs sub-tab` metadata
  block and `"bugs"` from the `next artifact` alias tuple.
- `src/sase/ace/tui/commands/_mode_commands.py`: drop the `"artifacts_bugs"` entry from
  `tab_to_command_tab`.
- `src/sase/ace/tui/modals/help_modal/patches_artifact_bindings.py`: delete the
  `"Bugs Pane"` box. `patches_copy_bindings.py`: delete the `Copy Mode · Bugs` box and
  the `cm.keys["artifacts_bugs"]` lookup. Keep every box at `_BOX_WIDTH = 57`, per
  `src/sase/ace/CLAUDE.md`.
- `src/sase/ace/tui/copy_targets.py`: delete the nine `artifacts_bugs` targets.
- Clipboard: drop the bugs branches in `actions/clipboard/_palette_registry.py`,
  `_palette.py` (`_ARTIFACT_SUBTABS`), `_palette_artifacts.py`,
  `_palette_artifact_previews.py`, `_artifact_references.py`,
  `_artifact_target_selected.py`, `_artifact_target_marked.py`, and
  `_artifact_target_values.py`. The `bug_agent_prompt` / `resolved_bug_url` imports die
  with those branches.
- `src/sase/ace/tui/widgets/_keybinding_modes.py`: drop `"bugs"` from the pane-key set
  in `update_copy_bindings`.
- `src/sase/ace/tui/actions/agent_workflow/_prompt_bar_mount.py`: drop the
  `subtab == "bugs"` branch.

### Repoint `@bug:` artifact-ref completion at the Beads pane

`_snapshot_artifact_ref_bug_candidates` in
`src/sase/ace/tui/widgets/_file_completion_base.py` reads
`query_one("#artifacts-bugs-pane").snapshot` (a `BugSnapshot`) to build
`ArtifactRefBugCandidate` rows for `@bug:` and `#ref/bug` completion. This is the one
non-mechanical piece of the deletion: the pane it reads is going away, and
`BUILTIN_ARTIFACT_REF_KINDS` still includes `bug`, so the completion must keep working.

Repoint it at `#artifacts-beads-pane`, whose `snapshot` is a `BeadsSnapshot` carrying
`external_projects: dict[str, ExternalIssueProjectCache]` — each cache has `project`,
`display_name`, and `issues: tuple[IssueWire, ...]`, which is everything the projection
needs. Walk `snapshot.external_projects.values()` instead of one pane-scoped snapshot,
matching `_xprompt_arg_assist_project_from_text()` against each cache's `display_name`
and `project`. Keep the existing `_artifact_ref_bug_projection` memo keyed on
`(snapshot identity, target project)` — `BeadsSnapshot` is replaced wholesale on each
load, so identity comparison stays a valid cache key, and this path must not do
per-keystroke work (see `sase/memory/tui_perf.md`).

Read `sase/memory/tui_perf.md` with `/sase_memory_read` before touching this function.

Cover it with a test: with a Beads pane holding an external-issue cache for a project,
`@bug:` completion offers that project's issues; with no cache, it returns no rows and
does not raise.

### Copy for humans

Update the sub-tab enumerations in `src/sase/ace/tui/widgets/tab_quickstart.py` (the
jump and cycle hints), `src/sase/ace/tui/widgets/patch_onboarding.py`
(`_PLACEHOLDER_COPY`-style descriptions and the "Browse commits, beads, bugs, PRs…"
line), and `src/sase/ace/tui/widgets/agent_onboarding.py`.

## Step 2 — Rename `prs` to `patches`

Canonical identifier is `patches`. Touch points, all of which already appear under
`rg -n '"prs"' src/`:

- `artifact_tabs.py`: `ArtifactsSubTab`, `ArtifactsPaneKey`, `ARTIFACTS_PANE_IDS`
  (`artifacts-prs-pane` → `artifacts-patches-pane`), `ARTIFACTS_ACCENTS`.
- `widgets/artifacts/panes.py`: `ArtifactsPrsPane` → `ArtifactsPatchesPane`, and the
  `subtab == "prs"` guard in `ArtifactPlaceholderPane.__init__`. Rename the class
  outright rather than keeping a class alias — an unused public alias is exactly what
  symvision rejects, and the compatibility contract is about the identifier string, not
  the type.
- `widgets/artifacts/view.py`: `_ARTIFACT_LABELS["patches"] = "Patches"`, the compose
  yield, and the `entry_navigator` guard (message: "Patches use the existing Patch
  navigation model").
- `widgets/artifacts/__init__.py` / `.pyi`, `widgets/__init__.py` / `.pyi`: re-export
  the renamed class.
- `styles.tcss`: `#artifacts-prs-pane` → `#artifacts-patches-pane`.
- `commands/catalog.py`: delete the `"PRs" if subtab == "prs"` special case —
  `subtab.title()` now yields `Patches`. Add `"prs"` to that command's `aliases` tuple
  so palette search for "prs" still finds it.
- `commands/context.py`, `commands/availability.py`, `commands/types.py`,
  `_app_action_availability.py`, `_app_watchers.py`, `actions/marking.py`,
  `actions/base.py`, `actions/artifacts.py`, `actions/artifacts_navigation.py`,
  `actions/navigation/_modals.py`, `actions/navigation/_fold.py`,
  `actions/event_refresh/_auto_refresh.py`, `actions/clipboard/_artifacts.py`,
  `actions/clipboard/_palette.py`, `actions/patch/_display.py`,
  `actions/patch/_query.py`, `actions/agents/_notification_navigation.py`,
  `widgets/patch_onboarding.py`, `modals/help_modal/patches_artifact_bindings.py`:
  `"prs"` → `"patches"`.
- `patch_onboarding.py:157`: `subtab.upper() if subtab == "prs" else subtab.title()`
  collapses to `subtab.title()`.
- `actions/artifacts_navigation.py`: `action_show_artifacts_prs` becomes
  `action_show_artifacts_patches`, with **`action_show_artifacts_prs` kept as a
  deprecated alias** delegating to it.

Add the string-level compatibility seam in `artifact_tabs.py` and use it at the two
entry points that accept an externally supplied sub-tab (`switch_to_artifacts_subtab`
and `_switch_artifacts_subtab`):

```python
LEGACY_ARTIFACTS_SUBTABS: dict[str, ArtifactsSubTab] = {
    "prs": "patches",   # legacy compatibility alias
    "bugs": "beads",    # retired sub-tab, routed to its replacement
}

def normalize_artifacts_subtab(value: str) -> ArtifactsSubTab: ...
```

Do **not** add `"patches"` to the pane-key set in
`_keybinding_modes.update_copy_bindings` — that set selects `artifacts_<pane>` copy
groups, and the Patches pane's group is `patches`, reached through the `tab` branch.
Adding it would look up a nonexistent `artifacts_patches` group. Leaving the set alone
keeps behavior byte-identical.

`NON_PRS_ARTIFACT_ACTIONS` and `_non_pr_artifacts_active()` keep their names in this
phase; they are internal and renaming them is orthogonal churn. Fix only the string
literals and the stale "the top-level internal id is still `patches`" comment in
`actions/artifacts.py`, which is now ambiguous with the sub-tab name — the top-level tab
id is `artifacts` (`ARTIFACTS_TAB`); `patches` and `changespecs` are its legacy tab ids.

## Step 3 — The silent renumber

Number keys come from `ARTIFACTS_SUBTAB_ORDER` in `src/sase/ace/tui/keymaps/bindings.py`
and again in `src/sase/ace/tui/bindings.py`, so removing `bugs` and reordering changes
every sub-tab's number with no error:

| key | before   | after       |
| --- | -------- | ----------- |
| `1` | Stitches | Stitches    |
| `2` | Beads    | **Patches** |
| `3` | Bugs     | **Beads**   |
| `4` | PRs      | **Files**   |
| `5` | Files    | —           |

Call this out explicitly in the commit message body. The strip already renders its
numbers (`show_numbers=True`), so the new mapping is discoverable at a glance; no code
change is needed for discoverability.

## Step 4 — Tests

- `tests/ace/tui/test_artifacts_scaffold.py`: assert
  `ARTIFACTS_SUBTAB_ORDER == ("stitches", "patches", "beads", "files")`, that no `bugs`
  pane or `#artifacts-bugs-pane` id exists, and that the Patches pane mounts as
  `#artifacts-patches-pane`.
- New regression coverage (extend the scaffold or keymap tests rather than adding a new
  file per assertion):
  - number keys `1`–`4` map to Stitches / Patches / Beads / Files and there is no `5`
    binding;
  - `action_show_artifacts_bugs()` lands on Beads;
  - `action_show_artifacts_prs()` and `switch_to_artifacts_subtab(app, "prs")` land on
    Patches;
  - a user config setting a retired Bugs keymap (`next_bug: "j"`) loads without a
    warning and without a `ValueError`.
- Mechanically update the `"prs"` → `"patches"` literals across `tests/` (85 files,
  overwhelmingly `expect_state("artifacts_subtab", "prs")`). Do this with a targeted
  search-and-replace and then re-read the diff — do not blanket-replace `prs` as a
  substring.
- Update `tests/test_keymaps_app_bindings.py`, `tests/test_keymaps_e2e.py`,
  `tests/test_command_availability_scope.py`,
  `tests/test_command_availability_changespecs.py`,
  `tests/ace/tui/test_artifacts_copy_mode.py`,
  `tests/ace/tui/test_artifacts_marking.py`,
  `tests/ace/tui/test_artifacts_list_navigation.py`,
  `tests/ace/tui/_artifacts_copy_helpers.py`,
  `tests/ace/tui/_copy_as_palette_helpers.py`, `tests/ace/tui/bench_artifacts_jk.py`,
  and the remaining files that enumerate the old five sub-tabs.
- Rename `tests/ace/tui/test_agents_jump_to_prs_subtab.py` and
  `tests/ace/tui/test_jump_all_entries_prs_subtab.py` to their `patches` spellings.

## Step 5 — Symvision

Read `sase/memory/symvision.md` with `/sase_memory_read`, then run
`just _lint-symvision`. Dismantling the pane will orphan public symbols. Apply that
memory's hierarchy in order — delete, then privatize, then a non-test pragma, and only
then `--epic-symbol` — and remember that a symbol used only by tests is not "used".
Expect candidates in the surviving `external_issues.py` module and in the clipboard and
command helpers whose only caller was a bugs branch. The Justfile's symvision invocation
currently carries no `--epic-symbol` entries; do not add one unless a later epic phase
genuinely consumes the symbol, which for the final phase of `sase-jd` means never.

## Step 6 — Goldens and docs

**PNG goldens.** Run `just test-visual`. Every snapshot showing the Artifacts sub-tab
strip changes (label set, order, and numbers), not only the Artifacts-pane ones. Inspect
the actual/expected/diff artifacts under `.pytest_cache/sase-visual/` **by eye** before
accepting anything, then rerun with `--sase-update-visual-snapshots`. No
`artifacts_prs_*` golden exists today, so the only golden renames are deletions of the
three `artifacts_bugs_*_120x40.png` files. No new glyph is introduced, so
`tests/ace/tui/visual/test_tab_icon_glyphs.py` needs no change.

**Docs.** Update every place the Artifacts sub-tabs are enumerated or the sub-tab is
named:

- `docs/ace.md` — the numbered strip (`1 Stitches · 2 Beads · 3 Bugs · 4 PRs · 5 Files`
  → `1 Stitches · 2 Patches · 3 Beads · 4 Files`), the `## Keybindings: Artifacts / PRs`
  heading, the copy-mode / marks / navigation section headings that list `Bugs`, the
  `Bugs` rows in the query-editor and copy-mode tables, and the `PRs sub-tab` phrasing.
  Three in-page links point at
  `#navigation-in-stitches-beads-plans-chats-other-and-bugs` — retarget them when the
  heading changes, and grep for any other anchor that embeds `bugs` or `prs`.
- `docs/configuration.md`, `docs/query_language.md` — `PRs sub-tab` → `Patches sub-tab`.
- `docs/blog/posts/why-coding-agents-need-orchestration.md`,
  `docs/blog/posts/structured-agentic-software-engineering.md`,
  `docs/images/sase_tui_tabs_infographic.prompt.md`,
  `docs/images/sase_tui_tabs_infographic.critique.md` — these enumerate the current
  sub-tab set and were updated by `sase-j8` for the same reason; update them the same
  way.

Leave alone every `PRs` that means an actual pull request (`Show/hide submitted PRs`
refers to Patch status, but `gh` pull requests in `docs/vcs.md` and
`docs/commit_workflows.md` do not describe the sub-tab). When in doubt, ask whether the
sentence would still be true if you substituted "Patches sub-tab"; if not, leave it.

## Verification

1. `just install`, then `just check`.
2. `just test-visual`, eyeball the diffs, `--sase-update-visual-snapshots`, re-run
   clean.
3. `just check-full` before landing (the epic plan requires it for this phase).
4. Manual smoke with `sase ace`: open Artifacts, confirm the strip reads
   `STITCHES PATCHES BEADS FILES` with numbers 1–4, that `5` does nothing, that `p`
   project scope still works on the three non-Patches panes, that the Beads pane still
   renders the external chip for bead `sase-jp`, and that the command palette still
   finds the Patches sub-tab when you type `prs`.
5. `rg -n '"bugs"|artifacts_bugs|artifacts-bugs|ArtifactsBugsPane|"prs"|artifacts-prs|ArtifactsPrsPane' src/`
   returns only the deliberate compatibility aliases (`LEGACY_ARTIFACTS_SUBTABS`,
   `action_show_artifacts_bugs`, `action_show_artifacts_prs`, the palette alias, and the
   `_RETIRED_APP_KEYS` entries).

### Known pre-existing failures — do not chase

- `tests/notification_store/test_mute_snooze.py` is a known flake under the full
  parallel lane (corroborated on `sase-ct` / `sase-h8`); it passes in isolation.
- `just check-full` is red on master for suite-cost summary budgets and two ACE/Textual
  cause budgets — tracked by task bead `sase-j0`. Confirm any failure you see is one of
  these by stashing your changes and re-running against clean `HEAD` before attributing
  it to this work.

Record anything else you discover as
`sase bead note sase-jd.8 'PROPOSED FOLLOW-UP: <one-line summary — detail>'`; do not
create beads from this phase.

## Follow-ups already identified

Record these as `PROPOSED FOLLOW-UP:` notes on `sase-jd.8` rather than fixing them here
— both predate this phase and neither is caused by the rename:

1. **Copy-mode footer is empty on the Patches pane.**
   `KeybindingFooter.update_copy_bindings` normalizes the tab to `"artifacts"` and, for
   the Patches pane, uses that as the copy-group key; but the group registered in
   `copy_targets.py` is `"patches"`, so `copy_targets_for("artifacts")` returns zero
   targets. `_normalize_copy_group` is a no-op (`"patches" if group == "patches"`), left
   over from the ChangeSpecs→Patches rename in `c7026e50e`. Verified by execution.
2. **Glossary entries for "external issue" and "PR origin"** are a deliberate epic-level
   non-goal needing the project owner's permission (already noted in the epic plan).

## Commit

One commit, conventional subject, with the renumber table in the body:

```text
feat(ace)!: retire the Bugs sub-tab and rename Artifacts PRs to Patches
```

The `!` is warranted: the Artifacts number keys silently change meaning, the Bugs pane
and its twelve keymap actions are gone, and `ace.keymaps.app.<bug action>` overrides are
now ignored. Deprecated aliases (`show_artifacts_bugs` → Beads, `show_artifacts_prs` →
Patches, `prs`/`bugs` sub-tab strings, retired keymap ids) keep saved state and existing
configs loading.
