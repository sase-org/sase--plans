- **PLAN:** [../202608/bead_notification_panel.md](../bead_notification_panel.md)

 Can you help me improve bead task notifications (see #sshot for what
these look like now)?

- Let's start replacing `[bead-task-triage] Task ready for triage:` with just
  `[bead]`.
- Let's start adding these to a new `Beads` notification panel tab (this should
  be configurable for any sase gate--if not make it so).
- Make sure that the agent that created the task bead is represented in both the
  sase gate action panel and the notification panel when a task bead
  notification is selected.
- I want you to lead the design on this one. Make sure you design this feature so it is intuitive, reliable, and (last but not least) beautiful!

Think this through thoroughly and create a plan using your `/sase_plan` skill. Choose and author the appropriate
tier, validate and revalidate until it passes, then submit it with `sase plan propose` (as the skill instructs)
before making any file changes.
 